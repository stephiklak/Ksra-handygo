<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- App Entry Point -->
        <activity
            android:name="com.ksra_handygo.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔐 Cognito Hosted UI Redirect Handler -->
        <activity
            android:name="com.ksra_handygo.auth.AuthActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <!-- Must match EXACT Cognito Redirect URI: ksrafisherman://callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="ksrafisherman"
                    android:host="callback" />
            </intent-filter>

            <!-- (Optional SAFE-PLUS) Deep link if later you want open login via URL -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="handygo"
                    android:host="login" />
            </intent-filter>

        </activity>

    </application>

</manifest>
