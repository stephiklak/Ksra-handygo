<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ksra_handygo">

    <!-- ✅ Internet permission added correctly -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- ✅ Launch activity -->
        <activity
            android:name=".auth.AuthActivity"
            android:exported="true">

            <!-- Launch screen -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ✅ Handle Cognito redirect back into app -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Must exactly match your Cognito callback URI -->
                <data
                    android:scheme="ksrafisherman"
                    android:host="callback" />
            </intent-filter>
        </activity>

        <!-- ✅ AppAuth internal redirect handler (required by AppAuth library) -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="ksrafisherman"
                    android:host="callback" />
            </intent-filter>
        </activity>

    </application>
</manifest>
